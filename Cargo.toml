[package]
name = "imageio"
version = "0.11.0"
edition = "2021"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/doom-fish/imageio-rs"
repository = "https://github.com/doom-fish/imageio-rs"
description = "Safe Rust bindings for Apple's ImageIO framework with a Swift bridge, typed property helpers, thumbnails, HEIF/APNG tooling, and optional raw FFI on macOS"
authors = ["Per Johansson <per@doom.fish>"]
keywords = ["image", "png", "jpeg", "heic", "macos"]
categories = ["api-bindings", "os::macos-apis", "multimedia::images"]
rust-version = "1.76"
readme = "README.md"
build = "build.rs"
include = [
    "src/**/*",
    "swift-bridge/Package.swift",
    "swift-bridge/Sources/**/*",
    "examples/**/*",
    "tests/**/*",
    "build.rs",
    "Cargo.toml",
    "README.md",
    "COVERAGE.md",
    "LICENSE-*",
    "CHANGELOG.md",
]

[features]
default = []
async = []
raw-ffi = []

[lints.rust]

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
cargo = { level = "warn", priority = -1 }

[lib]
name = "imageio"
crate-type = ["lib"]

[dependencies]
apple-cf = { version = ">=0.10, <0.11", default-features = false, features = ["cg"] }
doom-fish-utils = { version = ">=0.4, <0.5", default-features = false }

[dev-dependencies]
pollster = "0.3"
regex-lite = "0.1"

[package.metadata.docs.rs]
all-features = true
default-target = "aarch64-apple-darwin"
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]
rustdoc-args = ["--cfg", "docsrs"]

[badges]
maintenance = { status = "actively-developed" }

[[example]]
name = "11_async_incremental_decoder"
required-features = ["async"]

[[test]]
name = "animation_main_thread_tests"
path = "tests/animation_main_thread_tests.rs"
harness = false

[profile.release]
debug = "full"
strip = "none"
